import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        width: '100%',
        height: 120,
    },
    headerText: {
        color: '#fff',
        fontSize: 35,
        marginBottom: 10
    },
    regularText: {
        color: '#fff',
        fontSize: 15,
        fontWeight: '500'
    }
});